# 数据晾晒统计系统 - 前端开发计划

## 📋 文档说明

本文档详细规划了数据晾晒统计系统的前端开发任务,包括功能模块拆分、开发顺序、技术方案、关键组件设计等。

---

## 🎯 一、开发目标

### 1.1 核心目标

1. **自研核心组件**: 开发动态表格和动态表单组件,以满足长远的自定义需求
2. **复用 HDty 组件**: 最大化利用 `hd-date-picker`、`hd-organ`、`hd-region`、`hd-dict-*` 系列组件
3. **配置驱动渲染**: 基于配置动态渲染列表和表单,无需硬编码
4. **高性能展示**: 支持大数据量展示,分页加载,缓存优化
5. **良好扩展性**: 新增业务类别仅需配置,无需修改代码

### 1.2 技术栈

- **框架**: Vue 2.6.14
- **UI 库**: Element-UI 2.12.0
- **状态管理**: Vuex 3.6.0
- **路由**: Vue Router 3.0.1
- **HTTP 客户端**: Axios (axios-init)
- **组件使用说明**:

✅ **可直接复用的 HDty 组件**:
- `hd-date-picker` - 日期选择组件
- `hd-date-range-picker` - 日期范围选择组件
- `hd-organ` - 机构选择组件
- `hd-region` - 地区选择组件
- `hd-dict-select` - 字典下拉选择
- `hd-dict-cascader` - 字典级联选择
- `hd-dict-checkbox` - 字典多选框
- `hd-dict-radio` - 字典单选框

🔴 **需自研开发的核心组件**:
- **动态表格组件** (`DynamicTable`) - 不使用 `hd-build-table`,因为后期会有大量自定义需求
- **动态表单组件** (`DynamicForm`) - 不使用 `hd-form`,因为后期会有大量自定义需求,但**内部复用** HDty 字段组件
- **工具库**: Day.js, Lodash

---

## 📁 二、目录结构规划

### 2.1 目录设计原则

**⚠️ 重要提示**: 所有数据晾晒（flex-view）相关的代码都集中在 `src/views/business/flex-view/` 目录下，**不要在其他全局目录（如 src/components、src/api、src/store/modules）创建相关文件**，以免影响其他业务模块。

### 2.2 目录结构

```
src/views/business/flex-view/          # 数据晾晒模块根目录（所有代码都在这里）
│
├── stat/                              # 统计展示模块
│   ├── index.vue                      # 统计列表主页
│   └── components/                    # 统计页面专用组件
│       ├── StatTable.vue              # 统计表格组件（使用 DynamicTable）
│       ├── DetailDialog.vue           # 明细数据弹窗
│       └── StatQuery.vue              # 统计查询组件
│
├── config/                            # 配置管理模块
│   ├── column/                        # 列配置管理
│   │   ├── index.vue                  # 列配置主页
│   │   └── components/
│   │       ├── ColumnTree.vue         # 列树形编辑器
│   │       └── ColumnEditor.vue       # 列属性编辑器
│   │
│   └── form/                          # 表单配置管理
│       ├── index.vue                  # 表单配置列表
│       ├── form-edit.vue              # 表单编辑页
│       └── components/
│           ├── FormItemList.vue       # 表单项列表
│           └── FormItemEditor.vue     # 表单项编辑器
│
├── components/                        # 组件目录（flex-view 专用）
│   └── common/                        # 通用组件
│       ├── DynamicTable/              # 动态表格组件（核心）
│       │   ├── index.vue              # 主组件
│       │   ├── ColumnRender.vue       # 列递归渲染器
│       │   └── CellRender.vue         # 单元格渲染器
│       │
│       └── DynamicForm/               # 动态表单组件（核心）
│           ├── index.vue              # 主组件
│           ├── FormItem.vue           # 表单项组件
│           └── FieldRender.vue        # 字段渲染器（复用 HDty 组件）
│
├── api/                               # API 接口（flex-view 专用）
│   ├── column.js                      # 列配置 API
│   ├── form.js                        # 表单配置 API
│   ├── form-item.js                   # 表单项配置 API
│   ├── stat-data.js                   # 统计数据 API
│   └── biz-data.js                    # 业务数据 API
│
├── store/                             # Vuex 状态管理（flex-view 专用）
│   ├── column.js                      # 列配置状态
│   ├── form.js                        # 表单配置状态
│   └── stat.js                        # 统计数据状态
│
├── utils/                             # 工具函数（flex-view 专用）
│   ├── column-tree.js                 # 列树形结构处理
│   ├── form-render.js                 # 表单渲染工具
│   ├── stat-calc.js                   # 统计计算工具
│   └── validators.js                  # 自定义验证器
│
└── README.md                          # 模块说明文档
```

### 2.3 目录说明

**stat/**: 统计展示模块
- `index.vue`: 统计列表主页面
- `components/`: 统计页面专用组件（StatTable、DetailDialog、StatQuery）

**config/**: 配置管理模块
- `column/`: 列配置管理（列树形编辑器、列属性编辑器）
- `form/`: 表单配置管理（表单列表、表单编辑、表单项编辑）

**components/common/**: 通用组件（flex-view 模块内部复用）
- `DynamicTable/`: 动态表格组件（核心，自研）
- `DynamicForm/`: 动态表单组件（核心，自研）

**api/**: API 接口（flex-view 专用）
- 统一使用 axios-init 初始化
- 自动添加 token 和错误处理
- 接口路径前缀: `/api/flex-view/`

**store/**: Vuex 状态管理（flex-view 专用）
- 列配置缓存（5分钟）
- 表单配置缓存（5分钟）
- 统计数据缓存（1分钟）
- **注册方式**: 在 `src/store/index.js` 中导入并注册

**utils/**: 工具函数（flex-view 专用）
- `column-tree.js`: 列树形结构处理（扁平数组 ↔ 树形结构）
- `form-render.js`: 表单渲染工具（组件映射、配置解析）
- `stat-calc.js`: 统计计算工具（统计值计算、汇总行）
- `validators.js`: 自定义验证器（数字范围、日期范围）

### 2.4 模块隔离原则

✅ **DO（正确做法）**:
- 所有 flex-view 相关代码都在 `src/views/business/flex-view/` 目录下
- 使用相对路径导入本模块组件: `import DynamicTable from '../components/common/DynamicTable'`
- 使用 @ 别名导入 HDty 组件: `import HdDatePicker from '@/components/HdForm/Items/hd-date-picker'`

❌ **DON'T（错误做法）**:
- ❌ 不要在 `src/components/` 全局目录创建 flex-view 组件
- ❌ 不要在 `src/api/` 全局目录创建 flex-view 接口
- ❌ 不要在 `src/store/modules/` 全局目录创建 flex-view 状态
- ❌ 不要在 `src/utils/` 全局目录创建 flex-view 工具

---

## 🔧 三、核心功能开发

### 3.1 统计列表页面

#### 3.1.1 功能需求

1. ✅ 动态多级表头展示
2. ✅ 固定列(机构、总数)
3. ✅ 分页加载数据
4. ✅ 点击可配置列弹出明细
5. ✅ 查询条件筛选
6. ✅ 导出 Excel

#### 3.1.2 技术方案

**页面组件**: `src/views/business/flex-view/stat/index.vue`

```vue
<template>
  <div class="data-stat-page">
    <!-- 查询条件 -->
    <stat-query
      :query-fields="queryFields"
      @query="handleQuery"
      @reset="handleReset"
    />
    
    <!-- 统计表格 -->
    <stat-table
      ref="statTable"
      :columns="columns"
      :data="statData"
      :loading="loading"
      :pagination="pagination"
      @cell-click="handleCellClick"
      @page-change="handlePageChange"
    />
    
    <!-- 明细数据弹窗 -->
    <detail-dialog
      :visible.sync="detailVisible"
      :form-id="currentFormId"
      :org-id="currentOrgId"
      :column-prop="currentColumnProp"
      @refresh="loadStatData"
    />
  </div>
</template>

<script>
import { mapState, mapActions } from 'vuex'
import StatQuery from './components/StatQuery.vue'
import StatTable from './components/StatTable.vue'
import DetailDialog from './components/DetailDialog.vue'

export default {
  name: 'FlexViewStatPage',
  components: {
    StatQuery,
    StatTable,
    DetailDialog
  },
  data() {
    return {
      statData: [],
      loading: false,
      pagination: {
        current: 1,
        pageSize: 50,
        total: 0
      },
      queryParams: {},
      detailVisible: false,
      currentFormId: null,
      currentOrgId: null,
      currentColumnProp: null
    }
  },
  computed: {
    ...mapState('flexViewColumn', ['columns']),
    ...mapState('flexViewForm', ['formConfigs']),
    
    // 查询字段(从列配置中提取可查询的字段)
    queryFields() {
      return this.extractQueryFields(this.columns)
    }
  },
  created() {
    this.init()
  },
  methods: {
    ...mapActions('flexViewColumn', ['loadColumns']),
    ...mapActions('flexViewStat', ['loadStatData']),
    
    async init() {
      // 加载列配置
      await this.loadColumns()
      // 加载统计数据
      await this.loadData()
    },
    
    async loadData() {
      this.loading = true
      try {
        const params = {
          ...this.queryParams,
          page: this.pagination.current,
          pageSize: this.pagination.pageSize
        }
        const { data, total } = await this.loadStatData(params)
        this.statData = data
        this.pagination.total = total
      } catch (error) {
        this.$message.error('加载数据失败')
      } finally {
        this.loading = false
      }
    },
    
    // 查询
    handleQuery(params) {
      this.queryParams = params
      this.pagination.current = 1
      this.loadData()
    },
    
    // 重置
    handleReset() {
      this.queryParams = {}
      this.pagination.current = 1
      this.loadData()
    },
    
    // 单元格点击
    handleCellClick({ column, row }) {
      if (column.formId) {
        this.currentFormId = column.formId
        this.currentOrgId = row.orgId
        this.currentColumnProp = column.prop
        this.detailVisible = true
      }
    },
    
    // 分页切换
    handlePageChange(page) {
      this.pagination.current = page
      this.loadData()
    },
    
    // 提取查询字段
    extractQueryFields(columns) {
      const fields = []
      const traverse = (cols) => {
        cols.forEach(col => {
          if (col.queryable) {
            fields.push({
              prop: col.prop,
              label: col.label,
              type: col.queryType || 'input'
            })
          }
          if (col.children && col.children.length > 0) {
            traverse(col.children)
          }
        })
      }
      traverse(columns)
      return fields
    }
  }
}
</script>

<style lang="scss" scoped>
.data-stat-page {
  padding: 20px;
}
</style>
```

#### 3.1.3 统计表格组件 (DynamicTable)

**组件**: `views/data-overview/stat/components/StatTable.vue` 或 `components/data-overview/DynamicTable/index.vue`

**关键技术**:
- 🔴 **自研开发**,不使用 `hd-build-table`
- ✅ 基于 Element-UI 的 `el-table` 和 `el-table-column` 封装
- ✅ 递归渲染多级表头
- ✅ 单元格点击事件处理

```vue
<template>
  <div class="stat-table">
    <!-- 自研动态表格组件 -->
    <dynamic-table
      :columns="tableColumns"
      :data="data"
      :loading="loading"
      border
      stripe
      @cell-click="handleCellClick"
    >
      <!-- 自定义单元格渲染 -->
      <template #cell="{ row, column }">
        <span
          :class="{ 'clickable': column.formId }"
          :style="getCellStyle(row, column)"
          @click="handleClick(row, column)"
        >
          {{ formatValue(row[column.prop], column) }}
        </span>
      </template>
    </dynamic-table>
    
    <!-- 分页 -->
    <el-pagination
      :current-page="pagination.current"
      :page-size="pagination.pageSize"
      :total="pagination.total"
      :page-sizes="[20, 50, 100, 200]"
      layout="total, sizes, prev, pager, next, jumper"
      @current-change="handlePageChange"
      @size-change="handleSizeChange"
    />
  </div>
</template>

<script>
export default {
  name: 'StatTable',
  props: {
    columns: {
      type: Array,
      default: () => []
    },
    data: {
      type: Array,
      default: () => []
    },
    loading: {
      type: Boolean,
      default: false
    },
    pagination: {
      type: Object,
      default: () => ({
        current: 1,
        pageSize: 50,
        total: 0
      })
    }
  },
  computed: {
    // 转换列配置为 hd-build-table 格式
    tableColumns() {
      return this.buildTableColumns(this.columns)
    }
  },
  methods: {
    // 构建表格列配置
    buildTableColumns(columns) {
      return columns.map(col => {
        const column = {
          label: col.label,
          prop: col.prop,
          width: col.columnWidth,
          align: col.align || 'center',
          fixed: col.fixed,
          formId: col.formId
        }
        
        // 递归处理子列
        if (col.children && col.children.length > 0) {
          column.children = this.buildTableColumns(col.children)
        }
        
        return column
      })
    },
    
    // 单元格点击
    handleClick(row, column) {
      if (column.formId) {
        this.$emit('cell-click', { row, column })
      }
    },
    
    // 获取单元格样式
    getCellStyle(row, column) {
      const config = this.parseColumnConfig(column)
      let style = config.style || {}
      
      // 条件样式
      if (config.conditional && config.conditional.rules) {
        const value = row[column.prop]
        const rule = config.conditional.rules.find(r => {
          // 简单的条件判断(可扩展)
          return eval(r.condition.replace('value', value))
        })
        if (rule && rule.style) {
          style = { ...style, ...rule.style }
        }
      }
      
      return style
    },
    
    // 格式化值
    formatValue(value, column) {
      const config = this.parseColumnConfig(column)
      if (!config.formatter) return value
      
      const { type, precision, prefix, suffix } = config.formatter
      
      switch (type) {
        case 'number':
          return `${prefix || ''}${Number(value).toFixed(precision || 0)}${suffix || ''}`
        case 'percent':
          return `${Number(value).toFixed(precision || 2)}${suffix || '%'}`
        case 'date':
          return this.$dayjs(value).format(config.formatter.dateFormat || 'YYYY-MM-DD')
        default:
          return value
      }
    },
    
    // 解析列配置JSON
    parseColumnConfig(column) {
      try {
        return JSON.parse(column.columnConfig || '{}')
      } catch {
        return {}
      }
    },
    
    // 分页切换
    handlePageChange(page) {
      this.$emit('page-change', page)
    },
    
    handleSizeChange(size) {
      this.$emit('size-change', size)
    }
  }
}
</script>

<style lang="scss" scoped>
.stat-table {
  .clickable {
    color: $--color-primary;
    cursor: pointer;
    text-decoration: underline;
    
    &:hover {
      color: lighten($--color-primary, 10%);
    }
  }
}
</style>
```

---

### 3.2 明细数据弹窗

#### 3.2.1 功能需求

1. ✅ 动态加载表单配置
2. ✅ 列表展示明细数据
3. ✅ 新增/编辑/删除明细
4. ✅ 查询条件筛选
5. ✅ 分页加载

#### 3.2.2 技术方案

**组件**: `views/data-overview/stat/components/DetailDialog.vue`

```vue
<template>
  <el-dialog
    :title="`${orgName} - ${columnLabel}`"
    :visible="visible"
    width="80%"
    :close-on-click-modal="false"
    @close="handleClose"
  >
    <!-- 查询条件 -->
    <dynamic-form
      v-if="queryFields.length > 0"
      ref="queryForm"
      :form-items="queryFields"
      :inline="true"
      @submit="handleQuery"
    >
      <template #footer>
        <el-button @click="handleReset">重置</el-button>
        <el-button type="primary" @click="handleQuery">查询</el-button>
        <el-button type="success" @click="handleAdd">新增</el-button>
      </template>
    </dynamic-form>
    
    <!-- 数据列表 -->
    <el-table
      :data="dataList"
      border
      stripe
      v-loading="loading"
    >
      <el-table-column
        v-for="field in listFields"
        :key="field.id"
        :prop="field.itemProp"
        :label="field.itemLabel"
        :width="field.columnWidth"
      />
      <el-table-column label="操作" width="150" fixed="right">
        <template slot-scope="scope">
          <el-button size="mini" @click="handleEdit(scope.row)">编辑</el-button>
          <el-button size="mini" type="danger" @click="handleDelete(scope.row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>
    
    <!-- 分页 -->
    <el-pagination
      :current-page="pagination.current"
      :page-size="pagination.pageSize"
      :total="pagination.total"
      layout="total, prev, pager, next"
      @current-change="handlePageChange"
    />
    
    <!-- 新增/编辑弹窗 -->
    <el-dialog
      :title="editMode === 'add' ? '新增' : '编辑'"
      :visible.sync="editVisible"
      width="60%"
      append-to-body
    >
      <dynamic-form
        ref="editForm"
        :form-items="formFields"
        :init-data="currentRow"
        @submit="handleSave"
      >
        <template #footer>
          <el-button @click="editVisible = false">取消</el-button>
          <el-button type="primary" @click="$refs.editForm.submit()">保存</el-button>
        </template>
      </dynamic-form>
    </el-dialog>
  </el-dialog>
</template>

<script>
import DynamicForm from '@/components/data-overview/DynamicForm'
import { getFormConfig, getFormItems } from '@/api/data-overview/form'
import { getDataList, saveData, deleteData } from '@/api/data-overview/biz-data'

export default {
  name: 'DetailDialog',
  components: {
    DynamicForm
  },
  props: {
    visible: {
      type: Boolean,
      default: false
    },
    formId: String,
    orgId: String,
    columnProp: String
  },
  data() {
    return {
      formConfig: {},
      formFields: [],
      queryFields: [],
      listFields: [],
      dataList: [],
      loading: false,
      pagination: {
        current: 1,
        pageSize: 20,
        total: 0
      },
      editVisible: false,
      editMode: 'add',
      currentRow: {},
      queryParams: {}
    }
  },
  computed: {
    orgName() {
      // TODO: 从机构列表获取机构名称
      return this.orgId
    },
    columnLabel() {
      // TODO: 从列配置获取列标签
      return this.columnProp
    }
  },
  watch: {
    visible(val) {
      if (val) {
        this.init()
      }
    }
  },
  methods: {
    async init() {
      await this.loadFormConfig()
      await this.loadData()
    },
    
    // 加载表单配置
    async loadFormConfig() {
      try {
        const { data: config } = await getFormConfig(this.formId)
        const { data: items } = await getFormItems(this.formId)
        
        this.formConfig = config
        this.formFields = items.filter(f => f.showInForm === '1')
        this.queryFields = items.filter(f => f.showInQuery === '1')
        this.listFields = items.filter(f => f.showInList === '1')
      } catch (error) {
        this.$message.error('加载表单配置失败')
      }
    },
    
    // 加载数据
    async loadData() {
      this.loading = true
      try {
        const params = {
          formId: this.formId,
          orgId: this.orgId,
          ...this.queryParams,
          page: this.pagination.current,
          pageSize: this.pagination.pageSize
        }
        const { data, total } = await getDataList(params)
        
        // 合并 JSON 数据和映射字段
        this.dataList = data.map(item => ({
          ...item,
          ...JSON.parse(item.dataJson || '{}')
        }))
        this.pagination.total = total
      } catch (error) {
        this.$message.error('加载数据失败')
      } finally {
        this.loading = false
      }
    },
    
    // 查询
    handleQuery() {
      this.queryParams = this.$refs.queryForm.getFormData()
      this.pagination.current = 1
      this.loadData()
    },
    
    // 重置
    handleReset() {
      this.$refs.queryForm.resetFields()
      this.queryParams = {}
      this.pagination.current = 1
      this.loadData()
    },
    
    // 新增
    handleAdd() {
      this.editMode = 'add'
      this.currentRow = {}
      this.editVisible = true
    },
    
    // 编辑
    handleEdit(row) {
      this.editMode = 'edit'
      this.currentRow = { ...row }
      this.editVisible = true
    },
    
    // 保存
    async handleSave(formData) {
      try {
        await saveData({
          id: this.currentRow.id,
          formId: this.formId,
          orgId: this.orgId,
          ...formData
        })
        this.$message.success('保存成功')
        this.editVisible = false
        this.loadData()
        this.$emit('refresh')
      } catch (error) {
        this.$message.error('保存失败')
      }
    },
    
    // 删除
    async handleDelete(row) {
      try {
        await this.$confirm('确定删除该数据?', '提示', {
          type: 'warning'
        })
        await deleteData(row.id)
        this.$message.success('删除成功')
        this.loadData()
        this.$emit('refresh')
      } catch (error) {
        if (error !== 'cancel') {
          this.$message.error('删除失败')
        }
      }
    },
    
    // 分页
    handlePageChange(page) {
      this.pagination.current = page
      this.loadData()
    },
    
    // 关闭
    handleClose() {
      this.$emit('update:visible', false)
    }
  }
}
</script>
```

---

### 3.3 动态表单组件

#### 3.3.1 功能需求

1. ✅ 基于 hd-form 封装
2. ✅ 根据表单项配置动态渲染
3. ✅ 支持多种字段类型
4. ✅ 支持验证规则
5. ✅ 支持初始数据回填

#### 3.3.2 技术方案

**组件**: `components/data-overview/DynamicForm/index.vue`

```vue
<template>
  <hd-form
    ref="hdForm"
    :model="formData"
    :rules="formRules"
    :label-width="labelWidth"
    :inline="inline"
  >
    <el-row :gutter="20">
      <el-col
        v-for="item in formItems"
        :key="item.id"
        :span="getItemSpan(item)"
      >
        <el-form-item
          :label="item.itemLabel"
          :prop="item.itemProp"
        >
          <!-- 动态渲染字段 -->
          <field-render
            :item="item"
            :value="formData[item.itemProp]"
            @input="handleInput(item.itemProp, $event)"
          />
        </el-form-item>
      </el-col>
    </el-row>
    
    <!-- 操作按钮 -->
    <el-form-item v-if="!inline">
      <slot name="footer">
        <el-button @click="resetFields">重置</el-button>
        <el-button type="primary" @click="submit">提交</el-button>
      </slot>
    </el-form-item>
  </hd-form>
</template>

<script>
import FieldRender from './field-render.vue'

export default {
  name: 'DynamicForm',
  components: {
    FieldRender
  },
  props: {
    formItems: {
      type: Array,
      default: () => []
    },
    initData: {
      type: Object,
      default: () => ({})
    },
    labelWidth: {
      type: String,
      default: '120px'
    },
    inline: {
      type: Boolean,
      default: false
    }
  },
  data() {
    return {
      formData: {},
      formRules: {}
    }
  },
  watch: {
    formItems: {
      handler() {
        this.initForm()
      },
      immediate: true
    },
    initData: {
      handler(val) {
        this.formData = { ...this.formData, ...val }
      },
      deep: true
    }
  },
  methods: {
    // 初始化表单
    initForm() {
      this.initFormData()
      this.initFormRules()
    },
    
    // 初始化表单数据
    initFormData() {
      const data = {}
      this.formItems.forEach(item => {
        const config = this.parseConfig(item)
        data[item.itemProp] = config.defaultValue || null
      })
      this.formData = { ...data, ...this.initData }
    },
    
    // 初始化验证规则
    initFormRules() {
      const rules = {}
      this.formItems.forEach(item => {
        const config = this.parseConfig(item)
        const itemRules = []
        
        // 必填
        if (item.isRequired === '1') {
          itemRules.push({
            required: true,
            message: `${item.itemLabel}不能为空`,
            trigger: 'blur'
          })
        }
        
        // 从配置读取规则
        if (config.rules) {
          if (config.rules.maxLength) {
            itemRules.push({
              max: config.rules.maxLength,
              message: `长度不能超过${config.rules.maxLength}个字符`,
              trigger: 'blur'
            })
          }
          if (config.rules.pattern) {
            itemRules.push({
              pattern: new RegExp(config.rules.pattern),
              message: `${item.itemLabel}格式不正确`,
              trigger: 'blur'
            })
          }
        }
        
        if (itemRules.length > 0) {
          rules[item.itemProp] = itemRules
        }
      })
      this.formRules = rules
    },
    
    // 解析配置
    parseConfig(item) {
      try {
        return JSON.parse(item.itemConfig || '{}')
      } catch {
        return {}
      }
    },
    
    // 获取字段占位
    getItemSpan(item) {
      const config = this.parseConfig(item)
      return config.layout?.span || (this.inline ? 8 : 24)
    },
    
    // 输入处理
    handleInput(prop, value) {
      this.$set(this.formData, prop, value)
    },
    
    // 提交
    submit() {
      this.$refs.hdForm.validate(valid => {
        if (valid) {
          this.$emit('submit', this.formData)
        }
      })
    },
    
    // 重置
    resetFields() {
      this.$refs.hdForm.resetFields()
    },
    
    // 获取表单数据
    getFormData() {
      return this.formData
    },
    
    // 设置表单数据
    setFormData(data) {
      this.formData = { ...this.formData, ...data }
    }
  }
}
</script>
```

**字段渲染器**: `components/data-overview/DynamicForm/field-render.vue`

```vue
<template>
  <component
    :is="fieldComponent"
    :item="item"
    :config="itemConfig"
    :value="value"
    @input="$emit('input', $event)"
  />
</template>

<script>
export default {
  name: 'FieldRender',
  props: {
    item: {
      type: Object,
      required: true
    },
    value: {
      type: [String, Number, Date, Array],
      default: null
    }
  },
  computed: {
    // 解析配置
    itemConfig() {
      try {
        return JSON.parse(this.item.itemConfig || '{}')
      } catch {
        return {}
      }
    },
    
    // 选择组件
    fieldComponent() {
      const typeMap = {
        'input': 'el-input',
        'select': this.getSelectComponent(),
        'date': 'hd-date-picker',
        'textarea': 'el-input',
        'number': 'el-input-number',
        'organ': 'hd-organ',
        'region': 'hd-region',
        'dict': this.getDictComponent()
      }
      return typeMap[this.item.itemType] || 'el-input'
    }
  },
  methods: {
    // 获取下拉组件
    getSelectComponent() {
      // 判断是否使用字典
      if (this.item.dicType) {
        return 'hd-dict-select'
      }
      return 'el-select'
    },
    
    // 获取字典组件
    getDictComponent() {
      const config = this.itemConfig
      if (config.componentProps?.type === 'cascader') {
        return 'hd-dict-cascader'
      }
      if (config.componentProps?.type === 'checkbox') {
        return 'hd-dict-checkbox'
      }
      if (config.componentProps?.type === 'radio') {
        return 'hd-dict-radio'
      }
      return 'hd-dict-select'
    }
  }
}
</script>
```

---

## 📊 四、开发任务拆分

### 4.1 任务列表

| 任务编号  | 任务名称          | 优先级 | 工作量(人天) | 依赖任务            | 负责人    |
| --------- | ----------------- | ------ | ------------ | ------------------- | --------- |
| **FE-01** | **API 接口封装**  | 🔴 高   | 1            | -                   | 前端      |
| FE-01-01  | 列配置 API        | 🔴 高   | 0.3          | -                   | 前端      |
| FE-01-02  | 表单配置 API      | 🔴 高   | 0.3          | -                   | 前端      |
| FE-01-03  | 统计数据 API      | 🔴 高   | 0.2          | -                   | 前端      |
| FE-01-04  | 业务数据 API      | 🔴 高   | 0.2          | -                   | 前端      |
|           |                   |        |              |                     |           |
| **FE-02** | **Vuex 状态管理** | 🔴 高   | 1            | -                   | 前端      |
| FE-02-01  | 列配置状态        | 🔴 高   | 0.3          | -                   | 前端      |
| FE-02-02  | 表单配置状态      | 🔴 高   | 0.3          | -                   | 前端      |
| FE-02-03  | 统计数据状态      | 🔴 高   | 0.4          | -                   | 前端      |
|           |                   |        |              |                     |           |
| **FE-03** | **通用组件开发**  | 🔴 高   | 2            | -                   | 前端      |
| FE-03-01  | 动态表单组件      | 🔴 高   | 1            | -                   | 前端      |
| FE-03-02  | 字段渲染器        | 🔴 高   | 0.5          | FE-03-01            | 前端      |
| FE-03-03  | 统计查询组件      | 🟡 中   | 0.5          | -                   | 前端      |
|           |                   |        |              |                     |           |
| **FE-04** | **统计列表页面**  | 🔴 高   | 2.5          | FE-01, FE-02, FE-03 | 前端      |
| FE-04-01  | 统计列表主页      | 🔴 高   | 0.5          | -                   | 前端      |
| FE-04-02  | 统计表格组件      | 🔴 高   | 1            | -                   | 前端      |
| FE-04-03  | 明细弹窗组件      | 🔴 高   | 1            | FE-03-01            | 前端      |
|           |                   |        |              |                     |           |
| **FE-05** | **配置管理页面**  | 🟡 中   | 2.5          | FE-01, FE-02        | 前端      |
| FE-05-01  | 列配置管理页      | 🟡 中   | 1.5          | -                   | 前端      |
| FE-05-02  | 表单配置管理页    | 🟡 中   | 1            | -                   | 前端      |
|           |                   |        |              |                     |           |
| **FE-06** | **路由配置**      | 🟡 中   | 0.5          | -                   | 前端      |
|           |                   |        |              |                     |           |
| **FE-07** | **联调测试**      | 🔴 高   | 2            | 所有开发任务        | 前端+后端 |
|           |                   |        |              |                     |           |
| **总计**  |                   |        | **11.5**     |                     |           |

### 4.2 开发顺序

```
第1阶段 (2天): 基础设施搭建
├─ API 接口封装 (1天)
└─ Vuex 状态管理 (1天)

第2阶段 (2天): 通用组件开发
├─ 动态表单组件 (1天)
├─ 字段渲染器 (0.5天)
└─ 统计查询组件 (0.5天)

第3阶段 (2.5天): 核心页面开发
├─ 统计列表主页 (0.5天)
├─ 统计表格组件 (1天)
└─ 明细弹窗组件 (1天)

第4阶段 (2.5天): 配置管理页面
├─ 列配置管理页 (1.5天)
└─ 表单配置管理页 (1天)

第5阶段 (0.5天): 路由配置

第6阶段 (2天): 联调测试
```

---

## 🔍 五、关键技术点

### 5.1 hd-build-table 确认

**需要确认的问题**:
1. ❓ 是否支持多级表头递归渲染?
2. ❓ 是否支持动态列配置?
3. ❓ 是否支持单元格点击事件?

**预研方案**:
```javascript
// 1. 查看组件源码
// src/components/hd-build-table/index.vue

// 2. 测试示例
const columns = [
  {
    label: '实有人口',
    children: [
      { label: '总数', prop: 'total' },
      { label: '流动人口', prop: 'floating' }
    ]
  }
]
```

**备用方案**:
- 如果 `hd-build-table` 不支持,基于 `el-table` 自行封装

### 5.2 hd-form 确认

**需要确认的问题**:
1. ❓ 是否支持基于 JSON 配置动态渲染?
2. ❓ 是否支持自定义验证规则?
3. ❓ 是否支持字段联动?

**预研方案**:
```javascript
// 查看组件文档或源码
// src/components/hd-form/index.vue
```

### 5.3 性能优化

**大数据量优化**:
1. ✅ 分页加载(50条/页)
2. ✅ 虚拟滚动(如果需要)
3. ✅ 列配置缓存(Vuex)
4. ✅ 防抖/节流(查询、输入)

**渲染优化**:
1. ✅ 使用 `v-if` 代替 `v-show`(条件渲染)
2. ✅ 使用 `key` 优化列表渲染
3. ✅ 避免深层嵌套(扁平化数据)

---

## 📚 六、附录

### 附录A: HDty 框架组件清单

| 组件名           | 用途     | 文档路径                        |
| ---------------- | -------- | ------------------------------- |
| hd-form          | 动态表单 | src/components/hd-form          |
| hd-build-table   | 动态表格 | src/components/hd-build-table   |
| hd-date-picker   | 日期选择 | src/components/hd-date-picker   |
| hd-dict-select   | 字典选择 | src/components/hd-dict-select   |
| hd-dict-cascader | 字典级联 | src/components/hd-dict-cascader |
| hd-organ         | 机构选择 | src/components/hd-organ         |
| hd-region        | 区域选择 | src/components/hd-region        |
| hd-query         | 查询组件 | src/components/hd-query         |

### 附录B: 工具函数清单

| 工具函数      | 用途     | 文件路径                   |
| ------------- | -------- | -------------------------- |
| storage       | 本地存储 | src/utils/storage.js       |
| util-date     | 日期处理 | src/utils/util-date.js     |
| util-common   | 通用工具 | src/utils/util-common.js   |
| util-validate | 表单验证 | src/utils/util-validate.js |

---

**文档版本**: 1.0  
**编写日期**: 2025-11-26  
**适用版本**: HDty v2.0.1 + Vue 2.6.14  
**维护人员**: 前端团队
