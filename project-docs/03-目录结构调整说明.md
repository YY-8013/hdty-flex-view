# 数据晾晒模块目录结构调整说明

## 📋 文档说明

本文档记录了数据晾晒（flex-view）模块的目录结构调整过程和设计决策。

**创建时间**: 2025-11-26  
**调整原因**: 模块隔离，避免影响其他业务

---

## 🎯 调整目标

### 核心原则

**模块隔离**: 所有数据晾晒（flex-view）相关的代码都集中在 `src/views/business/flex-view/` 目录下，**不在全局目录（src/components、src/api、src/store/modules、src/utils）创建相关文件**。

### 调整原因

1. **避免影响其他业务模块**: 
   - 防止 flex-view 的组件、API、状态管理与其他业务模块混淆
   - 减少全局命名冲突的风险

2. **提高代码可维护性**:
   - 所有相关代码集中在一个目录，便于查找和修改
   - 模块边界清晰，职责明确

3. **便于后期迁移或删除**:
   - 如果需要移除或重构 flex-view 模块，只需操作一个目录
   - 不会遗留散落在各处的文件

---

## 📁 最终目录结构

### 完整目录树

```
src/views/business/flex-view/          # 数据晾晒模块根目录（所有代码都在这里）
│
├── stat/                              # 统计展示模块
│   ├── index.vue                      # 统计列表主页（个性化实现，不用 DynamicTable）
│   └── components/                    # 统计页面专用组件
│       ├── StatTable.vue              # 统计表格组件（基于 el-table，个性化定制）
│       ├── StatQuery.vue              # 统计查询组件
│       └── DetailDialog.vue           # 明细数据弹窗（内含明细列表和新增表单）
│
├── config/                            # 配置管理模块
│   ├── column/                        # 列配置管理
│   │   ├── index.vue                  # 列配置主页
│   │   └── components/
│   │       ├── ColumnTree.vue         # 列树形编辑器
│   │       └── ColumnEditor.vue       # 列属性编辑器
│   │
│   └── form/                          # 表单配置管理
│       ├── index.vue                  # 表单配置列表
│       ├── form-edit.vue              # 表单编辑页
│       └── components/
│           ├── FormItemList.vue       # 表单项列表
│           └── FormItemEditor.vue     # 表单项编辑器
│
├── components/                        # 组件目录（flex-view 专用）
│   └── common/                        # 通用组件
│       ├── DynamicTable/              # 动态表格组件（核心）
│       │   ├── index.vue              # 主组件
│       │   ├── ColumnRender.vue       # 列递归渲染器
│       │   └── CellRender.vue         # 单元格渲染器
│       │
│       └── DynamicForm/               # 动态表单组件（核心）
│           ├── index.vue              # 主组件
│           ├── FormItem.vue           # 表单项组件
│           └── FieldRender.vue        # 字段渲染器（复用 HDty 组件）
│
├── api/                               # API 接口（flex-view 专用）
│   ├── column.js                      # 列配置 API
│   ├── form.js                        # 表单配置 API
│   ├── form-item.js                   # 表单项配置 API
│   ├── stat-data.js                   # 统计数据 API
│   └── biz-data.js                    # 业务数据 API
│
├── store/                             # Vuex 状态管理（flex-view 专用）
│   ├── column.js                      # 列配置状态
│   ├── form.js                        # 表单配置状态
│   └── stat.js                        # 统计数据状态
│
├── utils/                             # 工具函数（flex-view 专用）
│   ├── column-tree.js                 # 列树形结构处理
│   ├── form-render.js                 # 表单渲染工具
│   ├── stat-calc.js                   # 统计计算工具
│   └── validators.js                  # 自定义验证器
│
└── README.md                          # 模块说明文档
```

### 目录层级说明

| 目录                 | 用途         | 说明                                         |
| -------------------- | ------------ | -------------------------------------------- |
| `stat/`              | 统计展示模块 | 统计列表主页及专用组件                       |
| `config/`            | 配置管理模块 | 列配置、表单配置的管理页面                   |
| `components/common/` | 通用组件     | flex-view 模块内部复用的组件                 |
| `api/`               | API 接口     | 所有后端接口调用，路径前缀 `/api/flex-view/` |
| `store/`             | Vuex 状态    | 列配置、表单配置、统计数据的缓存             |
| `utils/`             | 工具函数     | 树形结构处理、表单渲染、统计计算等工具       |

---

## 🔧 调整过程

### 1. 创建的目录

```bash
# 创建统计模块目录
src/views/business/flex-view/stat/
src/views/business/flex-view/stat/components/

# 创建配置管理目录
src/views/business/flex-view/config/column/
src/views/business/flex-view/config/column/components/
src/views/business/flex-view/config/form/
src/views/business/flex-view/config/form/components/

# 创建通用组件目录
src/views/business/flex-view/components/common/
src/views/business/flex-view/components/common/DynamicTable/
src/views/business/flex-view/components/common/DynamicForm/

# 创建 API 目录
src/views/business/flex-view/api/

# 创建 Vuex 状态管理目录
src/views/business/flex-view/store/

# 创建工具函数目录
src/views/business/flex-view/utils/
```

### 2. 删除的目录（错误创建）

在调整过程中，最初错误地在全局目录创建了以下文件夹，已全部删除：

```bash
# 已删除（不符合模块隔离原则）
src/components/flex-view/          ❌ 删除
src/api/flex-view/                 ❌ 删除
src/store/modules/flex-view/       ❌ 删除
```

---

## 📝 开发规范

### 1. 文件导入规范

#### ✅ 正确的导入方式

```javascript
// 1. 导入本模块组件（使用相对路径）
import DynamicTable from '../components/common/DynamicTable'
import DynamicForm from '../components/common/DynamicForm'
import { getColumnTree } from '../api/column'
import { arrayToTree } from '../utils/column-tree'

// 2. 导入 HDty 组件（使用 @ 别名）
import HdDatePicker from '@/components/HdForm/Items/hd-date-picker'
import HdOrgan from '@/components/HdForm/Items/hd-organ'
import HdRegion from '@/components/HdForm/Items/hd-region'
import HdDictSelect from '@/components/HdForm/Items/hd-dict-select'

// 3. 导入 Element-UI 组件
import { Table, TableColumn, Form, FormItem } from 'element-ui'

// 4. 导入第三方库
import dayjs from 'dayjs'
import _ from 'lodash'
```

#### ❌ 错误的导入方式

```javascript
// ❌ 不要使用 @ 别名导入本模块组件（路径过长）
import DynamicTable from '@/views/business/flex-view/components/common/DynamicTable'

// ❌ 不要从全局目录导入（全局目录不应有 flex-view 相关文件）
import DynamicTable from '@/components/flex-view/DynamicTable'
import { getColumnTree } from '@/api/flex-view/column'
```

### 2. Vuex 模块注册

在 `src/store/index.js` 中注册 flex-view 的状态模块：

```javascript
import Vue from 'vue'
import Vuex from 'vuex'

// 导入 flex-view 模块（注意路径）
import flexViewColumn from '@/views/business/flex-view/store/column'
import flexViewForm from '@/views/business/flex-view/store/form'
import flexViewStat from '@/views/business/flex-view/store/stat'

Vue.use(Vuex)

export default new Vuex.Store({
  modules: {
    // flex-view 模块
    flexViewColumn,   // 列配置状态
    flexViewForm,     // 表单配置状态
    flexViewStat      // 统计数据状态
  }
})
```

在组件中使用：

```javascript
import { mapState, mapActions } from 'vuex'

export default {
  computed: {
    ...mapState('flexViewColumn', ['columns']),
    ...mapState('flexViewForm', ['formConfigs']),
    ...mapState('flexViewStat', ['statData'])
  },
  methods: {
    ...mapActions('flexViewColumn', ['loadColumns']),
    ...mapActions('flexViewForm', ['loadFormConfigs']),
    ...mapActions('flexViewStat', ['loadStatData'])
  }
}
```

### 3. 路由配置

在 `src/router/modules/` 下创建 `flex-view.js`:

```javascript
/**
 * 数据晾晒（flex-view）模块路由
 */
export default [
  {
    path: '/flex-view',
    component: () => import('@/layout/index.vue'),
    meta: { title: '数据晾晒', icon: 'el-icon-data-analysis' },
    children: [
      {
        path: 'stat',
        name: 'FlexViewStat',
        component: () => import('@/views/business/flex-view/stat/index.vue'),
        meta: { title: '统计列表', icon: 'el-icon-s-data' }
      },
      {
        path: 'config/column',
        name: 'FlexViewColumnConfig',
        component: () => import('@/views/business/flex-view/config/column/index.vue'),
        meta: { title: '列配置', icon: 'el-icon-s-grid' }
      },
      {
        path: 'config/form',
        name: 'FlexViewFormConfig',
        component: () => import('@/views/business/flex-view/config/form/index.vue'),
        meta: { title: '表单配置', icon: 'el-icon-s-order' }
      },
      {
        path: 'config/form/edit/:id?',
        name: 'FlexViewFormEdit',
        component: () => import('@/views/business/flex-view/config/form/form-edit.vue'),
        meta: { title: '表单编辑', hidden: true }
      }
    ]
  }
]
```

### 4. API 接口规范

所有 API 接口路径前缀统一为 `/api/flex-view/`：

```javascript
// src/views/business/flex-view/api/column.js
import request from '@/utils/axios-init'

const API_PREFIX = '/api/flex-view'

// 获取列树形结构
export function getColumnTree(params) {
  return request({
    url: `${API_PREFIX}/column/tree`,
    method: 'get',
    params
  })
}

// 保存列配置
export function saveColumn(data) {
  return request({
    url: `${API_PREFIX}/column/save`,
    method: 'post',
    data
  })
}
```

---

## ⚠️ 注意事项

### 1. 模块隔离原则

- ✅ **DO**: 所有 flex-view 相关代码都在 `src/views/business/flex-view/` 目录下
- ❌ **DON'T**: 不要在 `src/components/`、`src/api/`、`src/store/modules/`、`src/utils/` 等全局目录创建 flex-view 相关文件

### 2. 组件复用

- ✅ **优先复用 HDty 组件**: `hd-date-picker`、`hd-organ`、`hd-region`、`hd-dict-*`
- 🔴 **核心组件自研**: `DynamicTable`、`DynamicForm`（后期会大改，需要自研）

### 3. 命名规范

**组件命名**:
- 页面组件: PascalCase，前缀 `FlexView`（如 `FlexViewStatPage`）
- 通用组件: PascalCase（如 `DynamicTable`）
- 子组件: PascalCase（如 `StatQuery`）

**文件命名**:
- 组件文件: PascalCase（如 `StatTable.vue`）
- 工具文件: kebab-case（如 `column-tree.js`）
- API 文件: kebab-case（如 `stat-data.js`）

**变量命名**:
- camelCase: 组件 data、props、methods（如 `statData`、`formConfig`）
- UPPER_SNAKE_CASE: 常量（如 `API_PREFIX`、`CACHE_EXPIRE`）

### 4. 代码注释

**必须添加注释的情况**:
- ✅ 复杂的业务逻辑
- ✅ 组件的 props、events、slots 说明
- ✅ 工具函数的参数、返回值说明（使用 JSDoc）
- ✅ API 接口的用途、参数、返回值说明

**JSDoc 示例**:
```javascript
/**
 * 将扁平数组转换为树形结构
 * @param {Array} array - 扁平数组
 * @param {String|Number} parentId - 父节点 ID（根节点传 null）
 * @returns {Array} 树形结构数组
 */
export function arrayToTree(array, parentId = null) {
  // ... 实现代码
}
```

---

## 📊 目录对比

### 调整前（错误方案）

```
src/
├── components/flex-view/          ❌ 全局组件目录（影响其他业务）
├── api/flex-view/                 ❌ 全局 API 目录（影响其他业务）
├── store/modules/flex-view/       ❌ 全局状态目录（影响其他业务）
├── utils/flex-view/               ❌ 全局工具目录（影响其他业务）
└── views/business/flex-view/      ⚠️ 只有页面文件
```

### 调整后（正确方案）

```
src/views/business/flex-view/      ✅ 所有代码都在这里
├── stat/                          ✅ 统计模块
├── config/                        ✅ 配置模块
├── components/                    ✅ 组件（flex-view 专用）
├── api/                           ✅ API 接口（flex-view 专用）
├── store/                         ✅ Vuex 状态（flex-view 专用）
└── utils/                         ✅ 工具函数（flex-view 专用）
```

---

## 📖 相关文档

- [模块 README](../src/views/business/flex-view/README.md)
- [前端开发计划](./02-前端开发计划.md)
- [需求可行性分析报告](./01-需求可行性分析报告.md)
- [系统架构设计文档](../dev-md/系统架构设计文档.md)

---

**文档版本**: v1.0  
**最后更新**: 2025-11-26  
**维护人**: 开发团队
