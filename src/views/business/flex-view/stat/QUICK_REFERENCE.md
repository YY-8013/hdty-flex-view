# 数据晾晒统计页面 - 快速参考

## 🎨 三主题快速切换

### 警用蓝主题（police）
```javascript
currentTheme: 'police'
```
- 适用：公安、政府部门
- 主色：蓝色 `#2563eb`
- Header：`#1e40af → #1d4ed8`

### 科技绿主题（green）
```javascript
currentTheme: 'green'
```
- 适用：环保、科技、健康
- 主色：绿色 `#059669`
- Header：`#047857 → #059669`

### 深邃黑主题（dark）
```javascript
currentTheme: 'dark'
```
- 适用：高端商务、科技感
- 主色：灰色 `#374151`
- Header：`#1f2937 → #111827`

## 📏 关键尺寸

### Header区域（56px）
- 高度：`56px`
- 标题字体：`18px`
- 标题图标：`22px`
- 用户信息：`14px`
- 字间距：`2px`

### 查询区域
- 内边距：`20px 24px`
- 表单字体：`13px`
- 表单项间距：`16px`

### 按钮区域
- 内边距：`12px 24px 16px`
- 背景色：`#fafbfc`
- 边框色：`#e8eaed`

### 表格区域
- 字体大小：`13px`
- 表头字重：`600`
- 表头内边距：`12px 0`
- 单元格内边距：`10px 0`

### 分页区域
- 内边距：`16px 24px`
- 背景色：`#fafbfc`
- 边框色：`#e8eaed`
- 按钮圆角：`4px`

## 🎯 样式文件结构

```
styles/
├── index.scss            # 基础样式（Header、主容器、分页）
├── theme-police.scss     # 警用蓝主题
├── theme-green.scss      # 科技绿主题
├── theme-dark.scss       # 深邃黑主题
├── theme-dropdown.scss   # 主题下拉菜单
├── stat-query.scss       # 查询组件样式
└── stat-table.scss       # 表格组件样式
```

## 🔧 样式导入

### 主页面（index.vue）
```scss
@import "./styles/index.scss";
@import "./styles/theme-police.scss";
@import "./styles/theme-green.scss";
@import "./styles/theme-dark.scss";
@import "./styles/theme-dropdown.scss";
```

### 查询组件（StatQuery.vue）
```scss
@import "../styles/stat-query.scss";
```

### 表格组件（StatTable.vue）
```scss
@import "../styles/stat-table.scss";
```

## 🎨 主题颜色速查

### 警用蓝（police）
| 位置 | 颜色 |
|------|------|
| Header渐变 | `#1e40af → #1d4ed8` |
| 查询背景 | `#eff6ff → #ffffff` |
| 表头渐变 | `#3b82f6 → #2563eb` |
| 按钮渐变 | `#3b82f6 → #2563eb` |
| 链接色 | `#2563eb` |
| Hover背景 | `#eff6ff` |

### 科技绿（green）
| 位置 | 颜色 |
|------|------|
| Header渐变 | `#047857 → #059669` |
| 查询背景 | `#f0fdf4 → #ffffff` |
| 表头渐变 | `#10b981 → #059669` |
| 按钮渐变 | `#10b981 → #059669` |
| 链接色 | `#059669` |
| Hover背景 | `#ecfdf5` |

### 深邃黑（dark）
| 位置 | 颜色 |
|------|------|
| Header渐变 | `#1f2937 → #111827` |
| 查询背景 | `#f9fafb → #ffffff` |
| 表头渐变 | `#4b5563 → #374151` |
| 按钮渐变 | `#4b5563 → #374151` |
| 链接色 | `#374151` |
| Hover背景 | `#f3f4f6` |

## 💡 常用动画参数

### 过渡时长
- 快速：`0.2s`
- 标准：`0.3s`
- 缓慢：`0.5s`

### 缓动函数
- 标准：`ease`
- 流畅：`cubic-bezier(0.4, 0, 0.2, 1)`
- 弹性：`ease-in-out`

### 常用动效
```scss
// 按钮hover
&:hover {
  transform: translateY(-1px);
}

// 可点击单元格
&:hover {
  opacity: 0.8;
  transform: scale(1.05);
}

// 下拉项hover
&:hover {
  transform: translateX(2px);
}
```

## 📝 快速修改指南

### 新增主题
1. 在 `styles/` 下创建 `theme-xxx.scss`
2. 复制现有主题文件内容
3. 修改颜色变量
4. 在 `index.vue` 中导入
5. 更新主题切换逻辑

### 调整尺寸
1. Header高度：修改 `styles/index.scss` 中 `.stat-header-bar { height: 56px }`
2. 字体大小：在对应样式文件中搜索 `font-size`
3. 内边距：在对应样式文件中搜索 `padding`

### 修改颜色
1. 找到对应主题文件（如 `theme-police.scss`）
2. 修改渐变色或纯色值
3. 保存即可实时生效

## 🔍 调试技巧

### 主题不生效
- 检查 `currentTheme` 值是否正确
- 检查样式文件是否正确导入
- 检查CSS类名是否正确（如 `.theme-police`）

### 样式覆盖问题
- 使用 `::v-deep` 穿透组件样式
- 提高选择器优先级
- 检查样式导入顺序

### 布局错乱
- 检查 Flexbox 布局属性
- 检查内边距和外边距
- 使用浏览器开发者工具调试

---

**文档版本**: v1.0.0  
**最后更新**: 2025-11-28
